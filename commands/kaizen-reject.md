---
description: Reject the kaizen approval the current run is waiting on — reason decides revise (back to planner) vs kill (abandon)
argument-hint: "<reason>"
---

Invoke the `kaizen` skill with argument `reject $ARGUMENTS`.
